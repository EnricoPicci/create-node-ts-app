const rootIndex = path.indexOf(_root);
return path.slice(rootIndex + _root.length + 1);

suggests the "rootIndex + _root.length + 1" parameter





getNpmCommands(template: string)

suggests the body of the function like this
    const commands = [];
    switch (template) {
        case 'default':
            commands.push('install');
            commands.push('start');
            break;
        default:
            throw new Error(`Unknown template: ${template}`);
    }
    return commands;

very close to what I needed





// the following code has been created automatically after the comment has been edited

        // check that the name in package.json has been set correctly
        getPackageJson('package.json').name.should.equal(appName);
